document.querySelector("#reply-<%= @reply.id %>").innerHTML = ("<%= j render 'form', path: reply_path(@reply), submit_msg: 'Save' %>");

$('.cancel-reply-btn').click(() => {
  document.querySelector("#reply-<%= @reply.id %>").innerHTML = ("<%= j render @reply %>");
});
