<%= form_with model: @reply, url: reply_path(@reply) do |f| %>
  <div class="field">
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= link_to "Cancel", "#", remote: true, class: "cancel-reply-btn" %>
    <%= f.submit "Save Reply" %>
  </div>
<% end %>
